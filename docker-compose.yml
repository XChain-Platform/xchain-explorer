version: '3'

services:
  xchain_explorer:
    build: .
    networks: 
      - network1
    ports:
      - "8080:8080"
    logging:
      driver: "json-file"
      options:
        max-size: "30m"
        max-file: "10"
        
networks:
  network1:
    name: xchain_network
    external: true